# 3. Результаты

## 3.1 Сводный рейтинг моделей

В данном разделе приведены количественные результаты широкого перебора, описанного в разделе 2.8.4: единое линейное семейство и девять нейросетевых архитектур, обучены раздельно для двух целевых переменных — лактатных порогов LT1 и LT2 — на пяти модальностях признаков и в двух вариантах состава (with_abs — с абсолютными признаками; noabs — без абсолютных признаков). Все метрики получены в рамках кросс-валидации с исключением одного испытуемого (leave-one-subject-out, LOSO) на n = 18 испытуемых (раздел 2.8.5) и приведены к субъект-взвешенной медианной средней абсолютной ошибке (mean absolute error, MAE) в минутах (раздел 2.9.1). В работе используются три дополняющих представления MAE: субъект-взвешенная медианная MAE (median per-subject MAE; основная метрика рейтингования), медиана |err| по окнам (window-median MAE) и raw window-weighted MAE (среднее по всем окнам без межсубъектного выравнивания). Коэффициент детерминации (R²) и ранговая корреляция Спирмена (ρ) вычислены согласно разделам 2.9.2 и 2.9.3 соответственно.

Главная сводка результатов представлена в таблице 3.1. Для каждой целевой переменной в неё включены три позиции: победитель в абсолютных метриках, мультимодальный аналог (на полном наборе признаков электромиографии (ЭМГ, EMG), ближней инфракрасной спектрометрии (NIRS) и вариабельности сердечного ритма (ВСР, HRV) — далее EMG+NIRS+HRV) и лучшая нейросетевая конфигурация.

**Таблица 3.1 — Финалисты по каждой целевой переменной (LOSO, n = 18; для отдельных нейросетевых конфигураций n = 17, раздел 2.8.5)**

| Таргет | Модель | Модальность, вариант | MAE_med, мин | ρ | R² |
|--------|--------|----------------------|--------------|------|------|
| LT2 | Ridge (линейное семейство) | HRV, with_abs | 1,90 | 0,91 | 0,82 |
| LT2 | ElasticNet (линейное семейство) | EMG+NIRS+HRV, with_abs | 1,91 | 0,91 | 0,83 |
| LT2 | ансамбль (вейвлет-TCN + ElasticNet) | EMG+NIRS+HRV, with_abs | 2,38 | 0,85 | 0,57 |
| LT1 | SVR (линейное семейство) | EMG+NIRS, with_abs | 2,11 | 0,74 | 0,52 |
| LT1 | SVR (линейное семейство) | EMG+NIRS+HRV, with_abs | 2,23 | 0,79 | 0,57 |
| LT1 | ансамбль (вейвлет-TCN + ElasticNet) | EMG+NIRS+HRV, noabs | 2,71 | 0,78 | 0,17 |

> Примечание. Внутренние модели линейного семейства зафиксированы в артефактах для всех пяти конфигураций: LT2 / HRV — Ridge (α = 1000); LT2 / EMG+NIRS+HRV — ElasticNet (α = 1,0; l1 = 0,9); LT1 / EMG+NIRS — SVR (C = 100, ε = 1,0; ядро RBF); LT1 / EMG+NIRS+HRV — SVR (C = 100, ε = 1,0; ядро RBF). Победителями LT2 оказались регуляризованная линейная регрессия (Ridge / ElasticNet), победителями LT1 — метод опорных векторов с радиальной базисной функцией. Это содержательное различие интерпретируется в разделе 3.9.

Из таблицы 3.1 непосредственно следует, что лучший достигнутый показатель по LT2 — субъект-взвешенная медианная MAE 1,90 мин при ρ = 0,91 и R² = 0,82, по LT1 — 2,11 мин при ρ = 0,74 и R² = 0,52. В обоих случаях победителем является конфигурация линейного семейства (раздел 2.8.6); статистическая проверка превосходства линейного семейства над нейросетевыми архитектурами вынесена в раздел 3.3.

Дополнительный показатель — выигрыш от постобработки фильтром Калмана (раздел 2.10): для победителя LT2 значение window-weighted raw MAE составляет 2,25 мин, после фильтра — 2,50 мин; для победителя LT1 — 3,52 мин и 3,59 мин соответственно. Фильтр Калмана не дал систематического выигрыша на победителях: его эффект статистически неоднороден и зафиксирован на рисунке распределения kalman_gain.

*Рисунок 3.1 — Сводный график «стабильность × MAE» для всех 164 конфигураций: ось абсцисс — субъект-взвешенная медианная MAE, ось ординат — IQR LOSO-ошибки между субъектами. Кандидаты-финалисты подписаны*

## 3.2 Диссоциация модальностей по порогам

Для оценки информативного вклада каждой модальности был построен лучший представитель на каждом наборе признаков (таблица 3.2). Сравнение проведено внутри одного класса модели — линейного семейства, что устраняет вклад различий в архитектуре и оставляет в качестве источника вариации только состав признаков.

**Таблица 3.2 — Лучшая модель по каждой модальности (линейное семейство, with_abs; субъект-взвешенная медианная MAE)**

| Модальность | LT1 MAE_med, мин | LT1 ρ | LT2 MAE_med, мин | LT2 ρ |
|-------------|------------------|-------|------------------|-------|
| EMG | 2,94 | 0,71 | 2,78 | 0,67 |
| NIRS | 3,62 | 0,72 | 3,45 | 0,62 |
| HRV | 3,51 | 0,65 | **1,90** | **0,91** |
| EMG+NIRS | **2,11** | **0,74** | 2,90 | 0,70 |
| EMG+NIRS+HRV | 2,23 | 0,79 | 1,91 | 0,91 |

В таблице 3.2 выявлена ключевая закономерность: оптимальная модальность зависит от целевой переменной. Для LT2 лучшие показатели даёт ВСР (1,90 мин); комбинирование её с ЭМГ и NIRS не улучшает результат (1,91 мин). Для LT1, напротив, ведущей является комбинация ЭМГ+NIRS (2,11 мин); добавление ВСР здесь незначительно ухудшает медиану (2,23 мин) при росте ρ (0,79 против 0,74). Сама по себе модальность ВСР для LT1 даёт MAE 3,51 мин — в 1,7 раза хуже, чем ЭМГ+NIRS.

Для верификации этого наблюдения проведены кросс-модальные парные сравнения по критерию Уилкоксона с поправкой Холма (раздел 2.11.1). Полная сводка приведена в таблице 3.3.

**Таблица 3.3 — Кросс-модальные парные сравнения победителей в категории with_abs (LOSO per-subject MAE, парный критерий Уилкоксона)**

| Таргет | Сравнение | Δ_median MAE, мин | p | Вердикт |
|--------|-----------|--------------------|------|---------|
| LT2 | HRV vs EMG+NIRS+HRV | −0,10 | 0,966 | эквивалентно |
| LT2 | HRV vs EMG+NIRS | −0,71 | 0,012 | HRV значимо лучше |
| LT1 | EMG+NIRS vs EMG+NIRS+HRV | +0,10 | 0,442 | эквивалентно |

**Таблица 3.3-bis — Кросс-модальные парные сравнения победителей в категории noabs (LOSO per-subject MAE, парный критерий Уилкоксона)**

| Таргет | Сравнение | Δ_median MAE, мин | p | Вердикт |
|--------|-----------|--------------------|------|---------|
| LT2 | EMG+NIRS+HRV vs HRV | −0,23 | 0,523 | эквивалентно |
| LT2 | EMG+NIRS+HRV vs EMG+NIRS | −0,37 | 0,325 | эквивалентно |
| LT2 | HRV vs EMG+NIRS | −0,49 | 0,393 | эквивалентно |
| LT1 | EMG+NIRS vs EMG+NIRS+HRV | +0,23 | 0,038 | EMG+NIRS+HRV значимо лучше |
| LT1 | EMG+NIRS vs HRV | −0,74 | 0,081 | тренд (EMG+NIRS) |

Полученные результаты количественно фиксируют:
1. В категории **with_abs** для LT2 одной модальности ВСР статистически достаточно (HRV vs EMG+NIRS+HRV p = 0,966), и ВСР значимо превосходит EMG+NIRS (p = 0,012); для LT1 достаточно ЭМГ+NIRS (добавление ВСР p = 0,442).
2. В категории **noabs** для LT2 все три модальности (HRV, EMG+NIRS, EMG+NIRS+HRV) статистически эквивалентны (p ≥ 0,32) — преимущество HRV из with_abs опирается именно на абсолютный признак *hrv_mean_rr_ms* (средний RR-интервал в окне, мс; здесь и далее имена признаков приводятся в форме внутренних идентификаторов, использованных в артефактах обучения, — полный глоссарий с расшифровкой и единицами измерения приведён в Приложении 1); для LT1 наблюдается обратная картина — добавление ВСР даёт значимое улучшение (EMG+NIRS+HRV vs EMG+NIRS, p = 0,038).

Таким образом, минимально достаточный набор сенсоров не универсален, а зависит как от целевого порога, так и от того, доступны ли индивидуальные базальные уровни (которые требуются для абсолютных признаков). Физиологическая интерпретация этой диссоциации вынесена в раздел 3.9.

*Рисунок 3.2 — Сравнение модальностей внутри линейного семейства: столбчатая диаграмма «модальность × MAE_med» с раздельной группировкой по LT1 и LT2; жирная рамка — победитель в целевой переменной*

*Рисунок 3.2-bis — Двухпанельный bar «модальность × MAE_med»: левая панель — with_abs (как Рис. 3.2), правая — noabs. Показывает, что в категории noabs преимущество HRV для LT2 исчезает (HRV 2,37; EMG+NIRS+HRV 2,35; EMG+NIRS 2,59) — все три модальности статистически эквивалентны (таблица 3.3-bis)*

## 3.3 Линейные модели против нейросетевых

Сопоставление линейного семейства (раздел 2.8.6) и девяти нейросетевых архитектур (раздел 2.8.7) выполнено по методике раздела 2.11.1 (парный Уилкоксон с поправкой Холма на per-subject MAE). Внутри каждой группы «целевая переменная × модальность × вариант» сравнивались все возможные пары версий, общее число пар — 648. По результатам анализа значимыми (p_holm < 0,05) оказались 168 пар из 648.

Сводка значимых побед линейного семейства над нейросетевыми архитектурами в категории with_abs представлена в таблице 3.4.

**Таблица 3.4 — Доминирование линейного семейства над нейросетевыми архитектурами в обеих категориях признаков (парный критерий Уилкоксона + Холм)**

| Группа (target × feature_set) | with_abs побед | with_abs поражений | noabs побед | noabs поражений |
|-------------------------------|----------------|--------------------|-------------|-----------------|
| LT1, EMG | 8 | 0 | 8 | 0 |
| LT1, EMG+NIRS | 8 | 0 | 8 | 0 |
| LT1, EMG+NIRS+HRV | 8 | 0 | 8 | 0 |
| LT1, NIRS | 0 | 0 | 5 | 0 |
| LT2, EMG | 5 | 0 | 2 | 0 |
| LT2, EMG+NIRS | 2 | 0 | 6 | 0 |
| LT2, EMG+NIRS+HRV | 8 | 0 | 7 | 0 |
| LT2, NIRS | 0 | 0 | 7 | 0 |
| **Суммарно** | **39** | **0** | **51** | **0** |

По результатам критерия Уилкоксона с поправкой Холма зафиксировано: линейное семейство статистически значимо превосходит все девять нейросетевых архитектур в 14 из 16 групп «целевая переменная × модальность × вариант». В категории with_abs — 39 значимых побед в 6 из 8 групп (в двух оставшихся группах LT1 / NIRS и LT2 / NIRS ни линейное семейство, ни нейросетевые модели не показывают значимого превосходства друг над другом, net = 0). В категории noabs линейное семейство значимо превосходит нейросетевые архитектуры во всех 8 группах (51 значимая победа, 0 поражений). Удаление абсолютных признаков, таким образом, не сужает преимущество линейного семейства, а расширяет его на конфигурации NIRS, в которых нейросети ранее соответствовали линейной опорной модели. Суммарно: **90 значимых побед, 0 значимых поражений в 14 из 16 групп**.

*Рисунок 3.3 — Тепловая карта p_holm попарных сравнений всех версий внутри группы LT2, EMG+NIRS+HRV, with_abs: красный — значимое превосходство строки над столбцом, синий — значимое поражение*

*Рисунок 3.4 — Аналогичная тепловая карта для группы LT1, EMG+NIRS, with_abs*

*Рисунок 3.3-bis — Тепловая карта p_holm для группы LT2, NIRS, noabs (8 значимых побед линейного семейства — типичный пример «закрытия» категории NIRS при удалении абсолютных признаков)*

*Рисунок 3.4-bis — Тепловая карта p_holm для группы LT1, NIRS, noabs (5 значимых побед линейного семейства)*

Зафиксированные количественные результаты разделов 3.1–3.3 формируют каркас для последующего анализа: характер обнаруженного доминирования линейных моделей и диссоциация модальностей по целевым переменным интерпретируются в разделе 3.9.

## 3.4 Абсолютные против трендовых признаков

В рамках широкого перебора каждая модель обучалась в двух вариантах состава признаков — with_abs и noabs (раздел 2.7.6; перечень удаляемых в noabs признаков — там же). В данном разделе количественно сопоставлены результаты обоих вариантов и установлено, в какой степени абсолютные признаки необходимы для предсказания. Сопоставление выполнено раздельно для нейросетевых архитектур и для линейного семейства, так как механизмы использования абсолютных уровней этих двух классов моделей различны (раздел 2.8.6 для линейной модели, 2.8.7 для нейросетевых).

### 3.4.1 Нейросетевые архитектуры

Для каждой из 72 нейросетевых конфигураций (9 архитектур × 4 модальности × 2 целевые переменные) проведено парное сравнение with_abs и noabs по критерию Уилкоксона со знаковыми рангами на per-subject MAE (раздел 2.11.1) с применением операциональной границы эквивалентности (раздел 2.11.3). Сводный результат приведён в таблице 3.5.

**Таблица 3.5 — Сводка абляции (ablation) *with_abs vs noabs* для нейросетевых архитектур (n_paired = 18 субъектов; критерий эквивалентности из раздела 2.11.3)**

| Вердикт | Число конфигураций | Доля |
|---------|--------------------|------|
| Эквивалентно | 62 | 86 % |
| Лучше noabs (p < 0,05) | 7 | 10 % |
| Лучше with_abs (p < 0,05) | 1 | 1 % |
| Неопределённо (большая |Δ|, p ≥ 0,05) | 2 | 3 % |
| **Итого** | **72** | **100 %** |

Медиана |Δ_median MAE| по всем 72 парам составляет 0,02 мин (≈ 1,2 с), что значительно ниже клинически различимого порога 0,3 мин, заданного в разделе 2.11.3.

Наиболее выраженные неэквивалентные случаи приведены в таблице 3.6.

**Таблица 3.6 — Конфигурации нейросетевых архитектур с вердиктом «лучше noabs» (Δ_median MAE < 0; p — двусторонний Уилкоксон без поправки в данной таблице приведён как есть)**

| Архитектура | Таргет | Модальность | Δ_median, мин | p |
|-------------|--------|-------------|----------------|-------|
| ансамбль | LT2 | NIRS | −0,55 | 0,013 |
| ансамбль | LT1 | EMG+NIRS | −0,35 | 0,010 |
| ансамбль | LT1 | EMG+NIRS+HRV | −0,33 | 0,008 |

Установлено: для нейросетевых архитектур абсолютные признаки не несут дополнительной информации. В 86 % конфигураций варианты with_abs и noabs статистически эквивалентны; из 10 неэквивалентных конфигураций в 7 случаях noabs значимо лучше, тогда как with_abs значимо лучше только в 1 случае (TCN на LT2 / EMG+NIRS+HRV, Δ = +0,20 мин, p = 0,016).

*Рисунок 3.5 — вулкан-график (volcano plot) абляции with_abs vs noabs по всем 72 нейросетевым конфигурациям: ось абсцисс — Δ_median MAE (с – with_abs, slope в сторону noabs отрицательный), ось ординат — −log10(p_Wilcoxon); выделение значимых конфигураций*

### 3.4.2 Линейное семейство

Для линейного семейства (по одному лучшему предсказателю на каждую конфигурацию *feature_set × target × variant* — раздел 2.8.6) абляция проведена по полной аналогии с нейросетевыми — парный критерий Уилкоксона на per-subject MAE с применением операциональной границы эквивалентности (раздел 2.11.3). Результаты по 10 парам — 5 модальностей × 2 целевые переменные — приведены в таблице 3.7.

**Таблица 3.7 — Полная абляция *with_abs vs noabs* для линейного семейства (LOSO per-subject MAE, n_paired = 18; критерий эквивалентности из раздела 2.11.3)**

| Таргет | Модальность | MAE_med with_abs, мин | MAE_med noabs, мин | Δ_median, мин | p | Вердикт |
|--------|-------------|------------------------|---------------------|----------------|------|---------|
| LT1 | EMG | 2,94 | 2,94 | 0,00 | — | N/A (нет абс. признаков) |
| LT1 | NIRS | 3,62 | 3,30 | −0,05 | 0,523 | эквивалентно |
| LT1 | HRV | 3,51 | 3,33 | +0,22 | 0,766 | эквивалентно |
| LT1 | EMG+NIRS | 2,11 | 2,36 | +0,13 | 0,304 | эквивалентно |
| LT1 | EMG+NIRS+HRV | 2,23 | 2,38 | +0,01 | 0,966 | эквивалентно |
| LT2 | EMG | 2,78 | 2,78 | 0,00 | — | N/A (нет абс. признаков) |
| LT2 | NIRS | 3,45 | 2,95 | −0,69 | 0,108 | неопределённо |
| LT2 | HRV | **1,90** | **2,37** | **+0,54** | **0,006** | **лучше with_abs** |
| LT2 | EMG+NIRS | 2,90 | 2,59 | −0,12 | 0,119 | эквивалентно |
| LT2 | EMG+NIRS+HRV | **1,91** | **2,35** | **+0,53** | **0,034** | **лучше with_abs** |

Из таблицы 3.7 следует:
- для **LT1** все пять модальностей в линейной модели **эквивалентны** в выборе with_abs vs noabs (p ≥ 0,30; |Δ| ≤ 0,25 мин), независимо от того, есть ли в наборе абсолютные признаки и какова их доля;
- для **LT2** два набора показывают значимое преимущество with_abs — это HRV (Δ = +0,54 мин, p = 0,006) и мультимодальная EMG+NIRS+HRV (Δ = +0,53; p = 0,034); общий для обоих абсолютный признак — *hrv_mean_rr_ms*;
- для **LT2 / NIRS** направление обратное (Δ = −0,69 мин в пользу noabs), но не достигает значимости (p = 0,108) — это эффект, оставшийся неопределённым на текущей выборке;
- для **LT2 / EMG+NIRS** различия не значимы (p = 0,119).

Конформные интервалы на тех же четырёх финалистах (раздел 3.8, таблица 3.17) количественно подтверждают эту картину: для LT2 / HRV полуширина 80 % растёт с ±3,49 до ±4,87 мин при удалении абсолютных, для LT1 / EMG+NIRS — сужается с ±6,99 до ±6,59 мин.

Стандартизованные коэффициенты Ridge на конфигурации, где эффект *hrv_mean_rr_ms* максимален (HRV / LT2, with_abs), сведены в таблице 3.8.

**Таблица 3.8 — Стандартизованные коэффициенты Ridge α=1000 на HRV / LT2 (доля от суммарного |коэффициента|)**

| Признак | \|std_coef\| | Доля, % |
|---------|--------------|---------|
| *hrv_mean_rr_ms* (абсолютный) | 175,02       | 44,6 |
| *hrv_dfa_alpha1* (трендовый) | 108,31       | 27,6 |
| *hrv_sd1_sd2_ratio* | 27,83        | 7,1 |
| *hrv_sd2_ms* | 23,34        | 6,0 |
| *hrv_sdnn_ms* | 22,01        | 5,6 |
| *hrv_rmssd_ms* | 17,81        | 4,5 |
| *hrv_sd1_ms* | 17,81        | 4,5 |

Из таблицы 3.8 следует, что в модели линейного семейства абсолютный признак *hrv_mean_rr_ms* несёт 44,6 % суммарного вклада, тогда как на оставшиеся шесть вариабельностных/трендовых признаков приходится 55,3 % (главный из них — *hrv_dfa_alpha1* (DFA-α1, безразмерный показатель фрактальности коротковременной структуры RR-ряда) с долей 27,6 %).

### 3.4.3 Сводный вердикт

Установлено, что вердикт «нужны ли абсолютные признаки» зависит и от класса модели, и от целевой переменной:
- для **нейросетевых архитектур** абсолютные признаки не нужны: 86 % конфигураций эквивалентны вариантам noabs, среди значимо различающихся преобладают конфигурации в пользу noabs;
- для **линейного семейства на LT1** абсолютные признаки также не нужны: все пять модальностей эквивалентны (p ≥ 0,30);
- для **линейного семейства на LT2** абсолютные признаки критичны для двух конфигураций — HRV и мультимодальной EMG+NIRS+HRV (Δ_median MAE ≈ +0,54 мин, p ≤ 0,034). В обоих случаях ведущий абсолютный признак — *hrv_mean_rr_ms*, доля которого в коэффициентах Ridge на HRV / LT2 — 44,6 % (таблица 3.8), удаление снижает R² с 0,82 до 0,68.

Содержательная интерпретация механизма расхождения по целевым переменным и классам моделей — почему линейное семейство на LT2 опирается на абсолютный уровень *hrv_mean_rr_ms* как кросс-субъектный «якорь», а нейросетевые архитектуры в нём не нуждаются — вынесена в раздел 3.9.3.

*Рисунок 3.5-bis — Per-subject диаграмма рассеяния (scatter) MAE с абсолютными признаками vs без них для линейного семейства — 10 панелей (5 модальностей × 2 целевые переменные), 18 точек на панель; диагональ y=x. Визуально показывает индивидуальные сдвиги, которые остаются за медианными значениями таблицы 3.7*

## 3.5 Структура ошибки предсказания

Сводные метрики разделов 3.1–3.2 характеризуют среднее качество предсказания, но не описывают, каким образом ошибка распределена между испытуемыми и между моментами теста. В данном разделе количественно охарактеризованы две оси неоднородности ошибки: (а) распределение per-subject MAE между подгруппами испытуемых и (б) зависимость ошибки от времени до порога. Все три топовые модели каждой целевой переменной из раздела 3.1 анализируются совместно.

### 3.5.1 Per-subject распределение ошибки и эффект подгруппы

Для проверки гипотезы о различии предсказуемости тренированных и нетренированных испытуемых per-subject MAE сравнивалась между двумя подгруппами выборки в соответствии с разбиением, заданным в разделе 2.8.9 (6 тренированных, 12 нетренированных); статистический тест — Манн–Уитни (раздел 2.11.2). Результаты сведены в таблице 3.9.

**Таблица 3.9 — Распределение per-subject MAE по подгруппам (MAE_med, мин; критерий Манна–Уитни, двусторонний)**

| Модель | Таргет | MAE тренированные (n = 6) | MAE нетренированные (n = 12) | p |
|--------|--------|----------------------------|-------------------------------|------|
| Ridge / HRV | LT2 | 1,97 | 1,90 | 0,682 |
| ElasticNet / EMG+NIRS+HRV | LT2 | 2,02 | 1,88 | 0,437 |
| ансамбль / EMG+NIRS+HRV | LT2 | 2,66 | 2,07 | 0,350 |
| SVR / EMG+NIRS | LT1 | **5,42** | **1,86** | **0,0008** |
| SVR / EMG+NIRS+HRV | LT1 | **4,81** | **1,66** | **0,0002** |
| ансамбль / EMG+NIRS+HRV (noabs) | LT1 | **4,54** | **2,15** | **0,0032** |

Из таблицы 3.9 непосредственно следует: для LT2 значимых различий per-subject MAE между подгруппами не обнаружено (p = 0,35…0,68); для LT1 во всех трёх топовых моделях тренированные предсказываются значимо хуже нетренированных (p = 0,0002…0,003); медианная per-subject MAE на тренированных в 2,5–3 раза выше, чем на нетренированных.

Дополнительно: кросс-целевая корреляция per-subject MAE — *MAE_LT1* против *MAE_LT2* по 18 испытуемым на конфигурации EMG+NIRS+HRV линейного семейства (SVR для LT1, ElasticNet для LT2) — статистически не значима (ρ = +0,20, p = 0,43). Зафиксировано: высокая ошибка на LT1 не сопровождается высокой ошибкой на LT2 у тех же испытуемых.

*Рисунок 3.6 — Гистограммы per-subject MAE по топовым моделям для LT1 и LT2; визуально различимый сдвиг распределения для LT1 у подгруппы тренированных*

*Рисунок 3.7 — ящик с усами (boxplot) per-subject MAE по подгруппам тренированных и нетренированных для всех шести топовых моделей*

### 3.5.2 Время-разрешённый анализ ошибки

Для каждой топовой модели был выполнен расчёт медианной абсолютной ошибки по бинам времени до порога (раздел 2.8.5; границы бинов: 0–30, 30–60, 60–120, 120–240, 240–480, > 480 с до порога). Результат приведён в таблице 3.10.

**Таблица 3.10 — Медианная абсолютная ошибка предсказания, мин, по бинам времени до порога (на основе window-wise оценок)**

| Модель | 0–30 с | 30–60 с | 60–120 с | 120–240 с | 240–480 с | > 480 с |
|--------|--------|----------|-----------|-----------|-----------|---------|
| Ridge / HRV (LT2) | 2,53 | 2,31 | 2,05 | 2,13 | **1,67** | 2,02 |
| ElasticNet / EMG+NIRS+HRV (LT2) | 1,51 | 1,64 | 1,88 | 1,87 | 1,83 | 2,25 |
| ансамбль / EMG+NIRS+HRV (LT2) | 2,33 | 2,05 | 2,13 | 2,12 | 2,01 | 2,51 |
| SVR / EMG+NIRS (LT1) | 1,69 | 2,33 | 2,29 | 1,58 | 1,87 | **7,13** |
| SVR / EMG+NIRS+HRV (LT1) | 1,75 | 2,03 | 2,26 | 1,62 | 2,77 | **6,74** |
| ансамбль / EMG+NIRS+HRV (LT1) | 1,99 | 2,08 | 1,46 | 1,98 | 2,69 | **6,21** |

Из таблицы 3.10 следует: для LT2 во всех трёх моделях медианная ошибка лежит в диапазоне 1,5–2,5 мин на всех бинах времени до порога; для LT1 во всех трёх моделях ошибка остаётся в диапазоне 1,5–2,8 мин на бинах вплоть до 240–480 с, а в бине > 480 с до порога возрастает до 6,2–7,1 мин.

Структурная связь с per-subject анализом раздела 3.5.1: бин > 480 с до LT1 формируется преимущественно из окон тренированных испытуемых, чьи тесты протяжённее и у которых начало теста удалено от LT1 на 8–15 мин. Дополнительная количественная оценка: при разделении окон LT1 (SVR / EMG+NIRS) на тренированных и нетренированных, 79 % окон с |err| > 7 мин приходятся на подгруппу тренированных при их 44 % доле в общем числе окон; mean |err| у тренированных составляет 5,82 мин, у нетренированных — 3,04 мин.

*Рисунок 3.8 — Кривые медианной ошибки по бинам времени до порога для LT1 и LT2 (по три модели на целевую переменную)*

### 3.5.3 Сводное наблюдение

Зафиксировано: для LT2 структурно выделенной подгруппы повышенной ошибки не обнаружено, медианная ошибка стабильна на всех временных бинах. Для LT1 значимое смещение per-subject MAE между тренированными и нетренированными (Манн–Уитни p ≤ 0,003) согласуется со скачком ошибки в бине > 480 с до порога: оба эффекта отражают одну и ту же подгруппу испытуемых. Это даёт основание для проверки гипотезы о целесообразности разделения модели на двух специалистов для LT1, изложенной в разделе 3.6. Для LT2 такое разделение содержательно не оправдано и не проверялось.

## 3.6 Система двух специалистов для LT1

В разделе 3.5 установлено, что подгруппа тренированных испытуемых для целевой переменной LT1 предсказывается значимо хуже нетренированных. На основании этого факта была проверена гипотеза о двух специалистах: раздельные модели для тренированных (n = 6) и нетренированных (n = 12) дают MAE ниже, чем единая обобщённая модель. Постановка эксперимента, разметка подгрупп и архитектура маршрутизатора описаны в разделе 2.8.9; перечень базальных признаков маршрутизатора — в разделе 2.7.7. Все системы построены на единой базовой модели — Ridge (α = 1000) на модальности EMG+NIRS (раздел 2.8.9), что устраняет различия в архитектуре между «специалистами» и обобщённой моделью (generalist). Это не победитель LT1 из раздела 3.1 (фактический победитель LT1 — SVR на EMG+NIRS), а методологически удобная регуляризованная линейная регрессия, выбранная по LOSO на полной выборке. Все системы валидированы LOSO по 18 испытуемым.

Сравнивались пять конфигураций:
- generalist — обобщённая модель, единая на всех 18 испытуемых;
- oracle_sym — симметричный вариант: каждый испытуемый направляется к специалисту своей истинной подгруппы (тренированный → специалист на 6 тренированных; нетренированный → специалист на 12 нетренированных);
- real_router_sym — то же, но с реальной маршрутизацией по логистической регрессии на базальных признаках;
- oracle_asym — асимметричный вариант: нетренированный → специалист на 12 нетренированных; тренированный → generalist;
- real_router_asym — асимметричный с реальной маршрутизацией.

Результаты приведены в таблице 3.11.

**Таблица 3.11 — Сравнение систем «два эксперта» для LT1 (LOSO, n = 18; MAE_med, мин — субъект-взвешенная медианная MAE; vs_generalist_p — двусторонний парный критерий Уилкоксона против generalist)**

| Система | MAE_med, мин | MAE_mean, мин | Δ_med vs generalist | p |
|---------|------------|----------|--------------------------|------|
| generalist | 3,15 | 3,91 | — | — |
| oracle_sym | 2,91 | 3,70 | 0,53 | 0,246 |
| real_router_sym | 3,17 | 3,97 | 0,41 | 0,899 |
| oracle_asym | 2,91 | **3,42** | 0,14 | **0,034** |
| real_router_asym | **2,18** | 3,46 | 0,14 | **0,074** |

Полученные данные количественно фиксируют:
- симметричный вариант («два эксперта»: trained-специалист на 6 испытуемых + untrained-специалист на 12 испытуемых) не превосходит обобщённую модель ни при идеальной маршрутизации (oracle_sym, p = 0,246), ни при реальной (real_router_sym, p = 0,899);
- асимметричный вариант (untrained-специалист на 12 испытуемых + обобщённая модель для тренированных) значимо превосходит обобщённую модель при идеальной маршрутизации (oracle_asym, p = 0,034); при реальной маршрутизации достигнут тренд к значимости (real_router_asym, p = 0,074) при снижении субъект-взвешенной медианной MAE с 3,15 до 2,18 мин.

Дополнительная количественная оценка асимметричного варианта с реальной маршрутизацией: 9 испытуемых улучшились по per-subject MAE, 1 ухудшился, 8 без изменений. Единственный случай ухудшения — испытуемый S003 (тренированный, маршрутизатор ошибочно отнёс его к нетренированным).

Маршрутизатор реализован в виде логистической регрессии на одиннадцати базальных признаках, описанных в разделе 2.7.7. Метрики маршрутизатора приведены в таблице 3.12.

**Таблица 3.12 — Топ-5 признаков маршрутизатора по абсолютному значению стандартизованного коэффициента логрегрессии (LOSO)**

| Признак | \|std_coef\| |
|---------|--------------|
| *hrv_hr_baseline_bpm* | 0,98         |
| *body_fat_pct* | 0,79         |
| *age* | 0,74         |
| *nirs_smo2_baseline_mean* | 0,59         |
| *bmi* | 0,29         |

Точность маршрутизатора в LOSO составляет 66,7 % (12 из 18 испытуемых: тренированные 4 из 6, нетренированные 8 из 12). Базовая частота большего класса (нетренированные) — 12 / 18 = 66,7 %; точность маршрутизатора совпадает с этой границей. Анализ чувствительности при альтернативном пороге *time_to_lt2* (целевая переменная — время до достижения LT2, с; медиана выборки) показал статистически меньший эффект асимметричной системы (oracle_asym p = 0,169; real_router_asym p = 0,327).

*Рисунок 3.9 — ящик с усами per-subject MAE по пяти системам (generalist, oracle_sym, real_router_sym, oracle_asym, real_router_asym) для LT1*

Полученные результаты по системе двух специалистов для LT1 являются эксплоративными в связи с малой численностью подгруппы тренированных (n = 6) и точностью маршрутизатора на уровне базовой частоты классов; ограничения зафиксированы в разделе 3.9 и в заключении.

## 3.7 Интерпретация модели: анализ важности признаков (SHAP)

Для интерпретации линейного семейства на четырёх ключевых конфигурациях разделов 3.1–3.2 — LT2 / HRV, LT2 / EMG+NIRS+HRV, LT1 / EMG+NIRS, LT1 / EMG+NIRS+HRV — выполнен анализ важности признаков методом SHAP (раздел 2.11.5). Модель интерпретации выбиралась так, чтобы максимально соответствовать фактическому победителю линейного семейства для каждой конфигурации:

- LT2 / HRV и LT2 / EMG+NIRS+HRV — Ridge с линейный объяснитель (LinearExplainer). Для LT2 / HRV это совпадает с фактическим победителем, для LT2 / EMG+NIRS+HRV — приближение к ElasticNet в рамках регуляризованной линейной регрессии (фактические победители — см. примечание к табл. 3.1).
- LT1 / EMG+NIRS и LT1 / EMG+NIRS+HRV — SVR с радиальным базисным ядром (фактический победитель LT1); в качестве модельно-независимого объяснителя использован пермутационный объяснитель (PermutationExplainer) с фоновой выборкой 100 окон и тестовой 300 окон.

Различие в выборе объяснителя продиктовано различием в семействе модели-победителя: для LT2 победители линейные, для LT1 — нелинейный SVR. Линейная Ridge-проекция вкладов признаков для LT1-конфигураций приведена в Приложении А-2 для сопоставления; в качестве основного источника интерпретации LT1 используется именно SVR-пермутационный объяснитель. Мера важности признака и групповая агрегация по модальности — по разделу 2.11.5; состав групп (EMG, NIRS, HRV, кинематика, признаки взаимодействия) — по разделу 2.7.

### 3.7.1 LT2 / HRV

Распределение SHAP-важности по семи признакам модальности HRV для победителя LT2 приведено в таблице 3.13.

**Таблица 3.13 — SHAP-важность признаков для Ridge / HRV / LT2 (mean |SHAP|, доля в %)**

| Признак | mean \|SHAP\| | Доля, % |
|---------|---------------|---------|
| *hrv_mean_rr_ms* | 136,39        | 43,5 |
| *hrv_dfa_alpha1* | 98,36         | 31,4 |
| *hrv_sd1_sd2_ratio* | 20,70         | 6,6 |
| *hrv_sd2_ms* | 16,85         | 5,4 |
| *hrv_sdnn_ms* | 15,74         | 5,0 |
| *hrv_rmssd_ms* | 12,59         | 4,0 |
| *hrv_sd1_ms* | 12,59         | 4,0 |

Установлено, что два признака — *hrv_mean_rr_ms* (43,5 %) и *hrv_dfa_alpha1* (31,4 %) — формируют в сумме 75 % SHAP-важности. Остальные пять признаков ВСР дают по 4–7 % каждый. Структура SHAP-вкладов согласуется со структурой стандартизованных коэффициентов Ridge из раздела 3.4.2 (таблица 3.8): тот же признак *hrv_mean_rr_ms* имеет наибольший вес как по коэффициенту (44,6 %), так и по SHAP (43,5 %).

### 3.7.2 LT2 / EMG+NIRS+HRV

SHAP-важности, агрегированные по группам признаков, для мультимодальной модели LT2 приведены в таблице 3.14.

**Таблица 3.14 — SHAP-важность по группам признаков для Ridge / EMG+NIRS+HRV / LT2**

| Группа | mean \|SHAP\| | Доля, % |
|--------|---------------|---------|
| EMG | 325,26        | 40,3 |
| NIRS | 193,12        | 23,9 |
| HRV | 142,94        | 17,7 |
| Признаки взаимодействия | 116,89        | 14,5 |
| Кинематика | 28,74         | 3,6 |

В мультимодальной модели LT2 группа HRV занимает третье место по совокупной SHAP-важности (17,7 %), уступая EMG (40,3 %) и NIRS (23,9 %). При этом субъект-взвешенная медианная MAE мультимодальной модели LT2 (1,91 мин по таблице 3.1) не отличается значимо от чисто HRV-модели (1,90 мин; p = 0,966 по таблице 3.3).

### 3.7.3 LT1 / EMG+NIRS

Для победителя LT1 — SVR (C = 100, ε = 1,0; ядро RBF) на EMG+NIRS — топ-10 признаков по SHAP-важности (пермутационный объяснитель) приведён в таблице 3.15.

**Таблица 3.15 — Топ-10 признаков по SHAP-важности для SVR / EMG+NIRS / LT1 (mean |SHAP|, доля в %)**

| Признак | Группа | mean \|SHAP\| | Доля, % |
|---------|--------|---------------|---------|
| *trainred_hhb_std* | NIRS | 39,61         | 5,2 |
| *trainred_smo2_drop* | NIRS | 36,70         | 4,9 |
| *smo2_from_running_max* | NIRS | 34,09         | 4,5 |
| *hhb_from_running_min* | NIRS | 31,36         | 4,2 |
| *trainred_hhb_mean* | NIRS | 30,26         | 4,0 |
| *trainred_thb_std* | NIRS | 28,06         | 3,7 |
| *trainred_hbdiff_std* | NIRS | 27,85         | 3,7 |
| *trainred_smo2_mean* | NIRS | 18,75         | 2,5 |
| *z_vl_dist_rest_ratio_mid_high* | EMG | 16,74         | 2,2 |
| *z_vl_dist_load_rms* | EMG | 16,65         | 2,2 |

Распределение SHAP-важности по группам приведено в таблице 3.16.

**Таблица 3.16 — SHAP-важность по группам признаков для SVR / EMG+NIRS / LT1**

| Группа | mean \|SHAP\| | Доля, % |
|--------|---------------|---------|
| EMG | 407,53        | 54,0 |
| NIRS | 304,01        | 40,3 |
| Кинематика | 43,21         | 5,7 |

Зафиксировано: в модели SVR / LT1 / EMG+NIRS доминирующего признака нет — ведущий признак (*trainred_hhb_std* — стандартное отклонение деоксигемоглобина HHb по окну, Train.Red) имеет долю 5,2 %, а суммарная доля топ-10 признаков составляет 37,1 %. По группам: EMG 54,0 %, NIRS 40,3 %, кинематика 5,7 %. Сравнение с линейной Ridge-проекцией (Приложение А-2): порядок групп тот же (EMG > NIRS > кинематика), но в SVR-разложении доля NIRS выше (40,3 % против 33,7 % в Ridge-проекции), что отражает способность SVR улавливать нелинейные взаимодействия между нескольких NIRS-каналов (drop SmO₂, отклонения от running-экстремумов, накопленные средние).

Для мультимодальной модели LT1 / EMG+NIRS+HRV распределение SHAP-важности по группам — EMG 46,1 %, NIRS 31,2 %, HRV 11,6 %, признаки взаимодействия 6,8 %, кинематика 4,4 %. Доля HRV (11,6 %) численно ниже её доли в мультимодальной модели LT2 (17,7 %, таблица 3.14), но при этом ведущим отдельным признаком мультимодальной LT1-модели оказывается *hrv_mean_rr_ms* (4,9 % суммарной важности), а вторым — взаимодействие *feat_rr_per_watt* (отношение среднего RR-интервала к мощности нагрузки, мс/Вт) (4,5 %): SVR извлекает из HRV информативный сигнал, неуловимый линейной моделью LT1 / EMG+NIRS (Приложение А-2). Сумма топ-10 признаков мультимодальной модели — 34,9 %, доминирующего признака по-прежнему нет.

*Рисунок 3.10 — Роевой график (beeswarm) SHAP для Ridge / HRV / LT2: ось абсцисс — SHAP-значение, цветовая шкала — нормированное значение признака*

*Рисунок 3.11 — Роевой график SHAP для SVR / EMG+NIRS / LT1 (пермутационный объяснитель)*

*Рисунок 3.12 — столбчатая диаграмма SHAP по группам признаков для мультимодальной модели SVR / EMG+NIRS+HRV / LT1*

## 3.8 Оценка неопределённости (конформные интервалы)

Для тех же четырёх финальных моделей в варианте with_abs и для их noabs-аналогов построены предсказательные интервалы методом LOSO split-conformal (раздел 2.11.4) на двух уровнях покрытия: α = 0,1 (90 %) и α = 0,2 (80 %). Сводка по покрытию и ширине интервалов приведена в таблице 3.17.

**Таблица 3.17 — Эмпирическое покрытие и средняя полуширина LOSO split-conformal интервалов (Ridge α=1000, обе категории признаков); раздельно по подгруппам тренированных и нетренированных (мин)**

| Модель | Вариант | α | Целевое покрытие | Эмпир. покрытие | Полуширина, общая | Тренированные | Нетренированные |
|--------|---------|-----|------------------|------------------|-------------------|----------------|------------------|
| Ridge / HRV (LT2) | with_abs | 0,1 | 0,90 | 0,885 | 5,25 | 5,14 | 5,30 |
| Ridge / HRV (LT2) | with_abs | 0,2 | 0,80 | 0,800 | 3,49 | 3,48 | 3,50 |
| Ridge / HRV (LT2) | noabs    | 0,1 | 0,90 | 0,893 | 6,58 | 6,53 | 6,61 |
| Ridge / HRV (LT2) | noabs    | 0,2 | 0,80 | 0,797 | 4,87 | 4,84 | 4,88 |
| Ridge / EMG+NIRS+HRV (LT2) | with_abs | 0,1 | 0,90 | 0,878 | 5,57 | 5,52 | 5,60 |
| Ridge / EMG+NIRS+HRV (LT2) | with_abs | 0,2 | 0,80 | 0,803 | 4,18 | 4,14 | 4,19 |
| Ridge / EMG+NIRS+HRV (LT2) | noabs    | 0,1 | 0,90 | 0,898 | 6,18 | 6,15 | 6,19 |
| Ridge / EMG+NIRS+HRV (LT2) | noabs    | 0,2 | 0,80 | 0,802 | 4,76 | 4,69 | 4,79 |
| Ridge / EMG+NIRS (LT1) | with_abs | 0,1 | 0,90 | 0,913 | 9,05 | 8,81 | 9,17 |
| Ridge / EMG+NIRS (LT1) | with_abs | 0,2 | 0,80 | 0,828 | 6,99 | 6,81 | 7,08 |
| Ridge / EMG+NIRS (LT1) | noabs    | 0,1 | 0,90 | 0,912 | 8,92 | 8,61 | 9,08 |
| Ridge / EMG+NIRS (LT1) | noabs    | 0,2 | 0,80 | 0,831 | 6,59 | 6,37 | 6,70 |
| Ridge / EMG+NIRS+HRV (LT1) | with_abs | 0,1 | 0,90 | 0,907 | 8,66 | 8,31 | 8,83 |
| Ridge / EMG+NIRS+HRV (LT1) | with_abs | 0,2 | 0,80 | 0,837 | 6,33 | 6,12 | 6,43 |
| Ridge / EMG+NIRS+HRV (LT1) | noabs    | 0,1 | 0,90 | 0,901 | 8,39 | 8,15 | 8,50 |
| Ridge / EMG+NIRS+HRV (LT1) | noabs    | 0,2 | 0,80 | 0,831 | 6,27 | 5,92 | 6,45 |

Зафиксировано:
- эмпирическое покрытие отклоняется от целевого на 0,2–2,2 п.п. для всех 16 строк, что лежит в пределах статистической вариативности при n = 18; гарантия LOSO split-conformal (раздел 2.11.4) выполнена и для with_abs, и для noabs;
- для LT2 удаление абсолютных признаков расширяет интервалы существенно: для HRV полуширина 80 % растёт с ±3,49 до ±4,87 мин (+1,38 мин), для EMG+NIRS+HRV — с ±4,18 до ±4,76 мин (+0,58 мин). Это согласуется с зафиксированной в разделе 3.4.2 критичностью *hrv_mean_rr_ms* для линейного семейства LT2;
- для LT1 полуширины noabs и with_abs совпадают в пределах 0,4 мин (LT1 / EMG+NIRS: ±6,99 → ±6,59 — даже сужение; LT1 / EMG+NIRS+HRV: ±6,33 → ±6,27). Это согласуется с эквивалентностью with_abs / noabs для нейросетевых архитектур LT1 (раздел 3.4.1) и расширяет вывод: абсолютные признаки не нужны и для линейного предсказания LT1;
- полуширина интервалов в подгруппах тренированных и нетренированных близка во всех 16 строках: разница не превышает 0,5 мин.

Близость полуширины в двух подгруппах связана с тем, что конформная калибровка раздела 2.11.4 проведена глобально (по всем тестовым окнам LOSO без стратификации по подгруппам). Структурное смещение per-subject MAE между тренированными и нетренированными для LT1 (раздел 3.5.1) не выражено в виде различия конформных полуширин при глобальной калибровке.

Дополнительная количественная характеристика структуры ошибки LT1 на window-level: распределение |err| по окнам Ridge / EMG+NIRS / LT1 имеет 80-й перцентиль 7 мин и 90-й перцентиль 9 мин; 79 % окон с |err| > 7 мин — окна тренированных испытуемых. Эта же подгруппа описана в разделе 3.5.1 и в анализе системы двух специалистов в разделе 3.6.

*Рисунок 3.13 — Сводный график конформного покрытия и полуширины интервалов: четыре финалиста with_abs и четыре noabs-аналога × два уровня α*

## 3.9 Обсуждение

В разделах 3.1–3.8 зафиксированы факты, без сопоставления с литературой. В настоящем разделе количественные результаты интерпретируются и сопоставляются с независимыми данными из обзора литературы (глава 1).

### 3.9.1 Диссоциация модальностей по порогам

Диссоциация модальностей зафиксирована в разделе 3.2: для LT2 минимально достаточной модальностью является ВСР, для LT1 — комбинация ЭМГ+NIRS (числовые значения MAE, ρ и R² приведены в разделе 3.2). Эта картина воспроизводится и на уровне индивидуального вклада признаков. SHAP-анализ (раздел 3.7) согласуется с ней количественно: в мультимодальной модели LT1 (SVR с пермутационный объяснитель) группа HRV даёт 11,6 % суммарной важности, в мультимодальной модели LT2 (Ridge) — 17,7 %, а в моно-модели HRV для LT2 — 100 %, причём 75 % приходятся на *hrv_mean_rr_ms* и *hrv_dfa_alpha1*.

Этот результат содержательно согласуется с физиологическим разграничением двух порогов, обобщённым в разделе 1.1 обзора литературы. По данным мета-анализа Tanner и соавторов (Tanner и др., 2024), показатель DFA-α1 = 0,75 рассматривается как онлайн-маркер HRVT1 (стандартизованная средняя разность относительно VT1 = 0,08, статистически не значима), а DFA-α1 = 0,5 — как маркер HRVT2 с систематическим занижением относительно LT2/VT2. Schaffarczyk и Gronwald (2022) на выборке велосипедисток подтвердили согласованность HRVT1 с VT1 (ICC = 0,77; r = 0,81) и HRVT2 с VT2 (ICC = 0,84; r = 0,80). Полученная в настоящей работе высокая информативность ВСР для LT2 (R² = 0,82) и низкая для LT1 (R² = 0,38) согласуется с физиологическим механизмом: на участке между LT1 и LT2 происходит резкое подключение симпатической ветви автономной нервной системы (раздел 1.4 обзора), вследствие чего нелинейные характеристики RR-ряда — DFA-α1 и средний RR — несут максимальный информативный сигнал именно для LT2.

Применительно к LT1 наблюдаемое доминирование ЭМГ и NIRS согласуется с моделью ATHHb-ЭМГ Попова с соавторами (Попов, Кузнецов, 2019; Popov, Vinogradova, 2019), в которой совместный анализ NIRS на vastus lateralis и ЭМГ той же мышцы используется как индикатор момента вовлечения быстрых двигательных единиц. Racinais и соавторы (Racinais и др., 2014) на 25 велосипедистах показали, что ЭМГ-порог по vastus lateralis наступает уже после VT2 (89 ± 5 % VO₂max); это согласуется с зафиксированной в настоящей работе более высокой долей ЭМГ-вклада именно в LT1-модели (54,0 % SHAP-вклада в SVR / LT1 / EMG+NIRS против 40,3 % в Ridge / LT2 / EMG+NIRS+HRV) — для LT1 ЭМГ-сигнал на VL ещё не «насыщен», а для LT2 уже подключаются глобальные регуляторные механизмы, отражённые в ВСР. Wang и Yoshikawa (Wang, Yoshikawa, 2006) и Lin и Huang (Lin, Huang, 2020) показали, что точки перегиба ΔHHb и O₂Hb по NIRS соответствуют VT1 и VT2 с корреляцией 0,97–0,99; настоящее исследование подтверждает информативность NIRS, но количественно демонстрирует, что для LT2 эта модальность вытесняется ВСР, тогда как для LT1 она остаётся ведущей в комбинации с ЭМГ.

В обзоре литературы (раздел 1.6.4) центральный gap сформулирован как отсутствие в литературе систем, объединяющих ЭМГ, NIRS и ВСР в единый алгоритм онлайн-предсказания обоих порогов в скользящем окне. Полученная в настоящей работе диссоциация модальностей является непосредственным вкладом в закрытие этого gap: количественно показано, что мультимодальная архитектура нужна не для повышения точности, а для покрытия обоих порогов одним конвейером, при этом для каждого порога автоматически выбирается ведущая модальность.

### 3.9.2 Доминирование линейного семейства над нейросетевыми архитектурами при n = 18

В разделе 3.3 установлено: линейное семейство (раздел 3.1, примечание к табл. 3.1) статистически значимо превосходит все девять нейросетевых архитектур (LSTM, Attention-LSTM, TCN, Monotonic TCN, четыре конфигурации вейвлет-семейства, ансамбль) в 14 из 16 групп «целевая переменная × модальность × вариант» (подробная статистика раздел 3.3, табл. 3.4). Удаление абсолютных признаков не сужает преимущество линейного семейства, а расширяет его на конфигурации, в которых нейросети успевали соответствовать линейной опорной модели.

Зафиксированное доминирование не противоречит, а уточняет литературу по применению нейросетевых архитектур к физиологическим временным рядам. Zeng и соавторы (Zeng и др., 2022) на медицинском датасете из 8 599 пациентов с 89 признаками показали, что LSTM и GRU значимо превосходят Random Forest. Miura и соавторы (Miura и др., 2020) на выборке из 260 пациентов получили r = 0,875 между предсказанным VT и измеренным с помощью CNN на одноканальном ЭКГ. Amelard и соавторы (Amelard и др., 2021) успешно применили TCN при n = 22, однако с использованием метаболических сигналов (минутная вентиляция, дыхательный объём), недоступных в полевых условиях. Tomaszewski и Majorczyk (Tomaszewski, Majorczyk, 2024) на 183 протоколах CPET для предсказания аэробного порога получили лучший результат на Random Forest (R² = 0,645, MAE = 4,63 Вт), а не на нейросетевой архитектуре.

Содержательное расхождение: в работах Zeng и Miura размер выборки на 1–2 порядка превышает размер выборки настоящей работы (n = 18). В разделе 1.6.3 обзора литературы указано, что при n < 50 субъектов ансамблевые методы обычно предпочтительнее глубоких архитектур, и Rodu с соавторами (Rodu и др., 2024) подчёркивают, что в малых выборках обучение с учителем должно дополнять, а не заменять гипотетико-дедуктивный подход. Полученный в настоящей работе результат количественно подтверждает этот общий принцип: при n = 18 субъектов даже архитектурно специализированные модели для временных рядов (TCN с дилатациями [1, 2, 4, 8, 16], Monotonic TCN со штрафом монотонности, вейвлет-обогащённые сети) не превосходят регуляризованную линейную регрессию на ручных признаках. Доминирование линейного семейства зафиксировано двусторонним критерием Уилкоксона с поправкой Холма — это методологическая проверка, отсутствующая в большинстве работ области (раздел 1.6.4 обзора).

Дополнительное содержательное наблюдение касается состава победителей внутри самого линейного семейства (см. примечание к табл. 3.1): для LT2 победителями оказались регуляризованные линейные модели, для LT1 — модели с нелинейной компонентой (SVR, градиентный бустинг). Это согласуется с диффузностью SHAP-профиля LT1 (раздел 3.9.5): для целевой переменной с отсутствием доминирующего признака линейная регрессия не извлекает достаточно сигнала, и нелинейные методы из расширенного линейного семейства дают выигрыш в рамках того же класса моделей с ручными признаками. Принципиально это не меняет вывод о доминировании ручных признаков над временными рядами при n = 18, но уточняет внутреннюю структуру победителей.

### 3.9.3 Зависимость роли абсолютных признаков от класса модели

В разделе 3.4 установлено: для нейросетевых архитектур абсолютные признаки эквивалентны их отсутствию в подавляющем большинстве конфигураций; для линейного семейства эффект концентрирован на LT2 и проходит через признак *hrv_mean_rr_ms* (раздел 3.4.2, табл. 3.7–3.8). Та же асимметрия проявляется и в ширине предсказательных интервалов. Конформные интервалы (раздел 3.8) количественно фиксируют этот эффект на уровне предсказательной неопределённости: полуширина LT2 / HRV растёт с ±3,49 до ±4,87 мин при 80 %-покрытии, тогда как для LT1 / EMG+NIRS изменение полуширины не превышает 0,4 мин и направлено на сужение (с ±6,99 до ±6,59 мин).

Этот результат имеет два самостоятельных аспекта. Первый — методологический. В литературе по предсказанию порогов вопрос о роли абсолютных значений против трендов систематически не ставится: гипотеза 2.7.6 («модель на одних трендах не уступает модели с абсолютными уровнями») в обзоре литературы не сформулирована. Reinpõld и Rannama (Reinpõld, Rannama, 2024) сообщают об индивидуальной вариативности абсолютных уровней SmO₂ (двусторонняя билатеральная NIRS снижает её, R² = 0,86); van der Zwaard и соавторы (van der Zwaard и др., 2016) показали, что около 80 % межиндивидуальной вариативности SmO₂ объясняется толщиной подкожного жирового слоя (ATT). С точки зрения такой высокой межиндивидуальной вариативности абсолютных уровней, эквивалентность вариантов with_abs и noabs для нейросетевых архитектур — содержательно ожидаемый эффект: модели, способные реконструировать положение на шкале нагрузки из последовательности окон (раздел 2.8.7), не выигрывают от шумного абсолютного входа.

Второй аспект — расхождение между нейросетевыми и линейными моделями. Это расхождение объясняется механизмом обработки временного контекста: линейная модель в варианте per-window не имеет внутренней рекуррентной памяти и использует абсолютный уровень *hrv_mean_rr_ms* как кросс-субъектный «якорь» положения на шкале нагрузки; нейросетевые архитектуры видят последовательность окон (раздел 2.8.7) и реконструируют положение из контекста. В литературе обзора этот аспект систематически не обсуждается. В разделе 1.6.2 обзора рассмотрены аргументы в пользу рекуррентных и темпоральных свёрточных архитектур для удержания временного контекста, но без сопоставления с линейными моделями на ручных признаках в режиме per-window.

Практическое следствие зафиксированной зависимости — при выборе линейной модели абсолютные признаки и, следовательно, индивидуальная калибровка базальных уровней необходимы; при выборе нейросетевой архитектуры калибровка избыточна. В разделе 1.3 обзора литературы это уточнение существенно: van der Zwaard и соавторы (van der Zwaard и др., 2016) показали, что ATT-зависимость SmO₂ принципиально ограничивает межсубъектное использование абсолютных уровней, и для нейросетевых архитектур настоящее исследование количественно показывает, что эта проблема обходится автоматически.

### 3.9.4 Эффект подгруппы тренированных для LT1

В разделе 3.5 зафиксирован эффект подгруппы тренированных для LT1 (раздел 3.5.1): per-subject MAE значимо выше на тренированных, медианная MAE — в 2,5–3 раза выше. Этот эффект далее воспроизводится в проверке двухэкспертной схемы. В разделе 3.6 показано, что симметричная гипотеза «два эксперта» не подтверждается (p = 0,246 при идеальной маршрутизации); подтверждается асимметричный вариант (определения симметричной и асимметричной конфигураций приведены в разделе 3.6): парный критерий Уилкоксона относительно обобщённой модели даёт p = 0,034 при идеальной маршрутизации и p = 0,074 (тренд) при реальной маршрутизации, со снижением субъект-взвешенной медианной MAE с 3,15 до 2,18 мин. Точность маршрутизатора 66,7 % совпадает с базовой частотой большего класса.

Этот результат интерпретируется по двум направлениям. Первое — связь с физиологией LT1. В разделе 1.1 обзора литературы (Messonnier, Emhoff, 2013) указано, что у высокотренированных спортсменов узкое горлышко метаболического клиренса лактата смещено в сторону доставки кислорода к работающей мышце; у нетренированных лимитирующим является митохондриальный аппарат. Эта асимметрия делает LT1 в популяции тренированных физиологически менее «жёсткой» точкой: ЭМГ-порог по VL у них отсрочен (Racinais и др., 2014; ЭМГ-порог наступает после VT2). Соответственно, признаки ЭМГ+NIRS, обученные на доминирующей подгруппе нетренированных, не обобщаются на тренированных — что согласуется с зафиксированным в настоящей работе ростом MAE до 5,42 мин на тренированных против 1,86 мин на нетренированных (SVR / EMG+NIRS / LT1).

Второе — соотношение объём данных / размерность. В разделе 1.6.3 обзора литературы указано, что при n < 50 субъектов специализация модели на подгруппе из 6 испытуемых сама по себе становится источником переобучения. Результат раздела 3.6 количественно подтверждает это: trained-специалист, обученный на ≤ 6 субъектах LOSO, уступает обобщённой модели даже на своей же подгруппе (median MAE 6,13 против 5,32 у обобщённой модели). Иначе формулируя: при имеющемся объёме выборки персонализация модели не достигается через разделение её на специалистов — но достигается через исключение нетипичной подгруппы из обучающей выборки модели большинства (асимметричный вариант). Это методологическое уточнение, не представленное в литературе обзора применительно к задаче предсказания лактатных порогов.

### 3.9.5 Различная структура SHAP-вкладов: концентрированный LT2 и диффузный LT1

В разделе 3.7 установлен концентрированный SHAP-профиль LT2 (75 % важности на двух признаках, см. табл. 3.13) против диффузного LT1 (ведущий признак — 5,2 %, см. табл. 3.15–3.16). Сопоставление двух способов разложения вкладов уточняет картину: аналогичная Ridge-проекция (Приложение А-2) даёт качественно тот же порядок групп для LT1, однако SVR извлекает дополнительный сигнал из совместной динамики нескольких NIRS-каналов (drop SmO₂, отклонения от running-экстремумов), которому линейная проекция не может назначить вес.

Полученное распределение содержательно согласуется с физиологическим разграничением порогов из раздела 1.1 обзора литературы и из раздела 3.9.1. LT2 имеет в литературе количественно охарактеризованный нелинейный маркер — DFA-α1 = 0,5 (Rogers, Gronwald, 2022), валидированный в нескольких независимых исследованиях (Schaffarczyk, Gronwald, 2022; Tanner и др., 2024); LT1 подобного концентрированного маркера в литературе не имеет, и в разделе 1.1 указано, что лимитирующие факторы LT1 различаются между тренированными (доставка кислорода) и нетренированными (митохондриальная ёмкость). Зафиксированный в SHAP диффузный профиль LT1 является эмпирическим следствием этой физиологической неоднородности.

С позиций конвейера машинного обучения этот результат имеет следствие: предсказательная способность по LT1 ограничивается не качеством модели, а отсутствием концентрированного физиологического маркера; для последующих работ это указывает на необходимость поиска агрегированных признаков, способных синтезировать вклад множества слабых сигналов.

### 3.9.6 Гетерогенность выборки

Изначально целевой популяцией исследования планировались триатлонисты (раздел 1.7 обзора литературы). По итогам набора выборка оказалась гетерогенной: в неё вошли лица с различным уровнем физической подготовки — от регулярно занимающихся в зале до спортсменов циклических видов. Эта характеристика выборки имеет двойственное методологическое следствие.

С одной стороны, гетерогенность является ограничением: выборка отклонилась от исходно заявленной целевой популяции, в работе невозможно сопоставить полученные численные показатели с нормами лактатного порога, опубликованными для популяции триатлонистов (раздел 1.7; Fiedler, Woll, 2025; диапазоны VT1 220–300 Вт и VT2 290–380 Вт для профессионалов); подгруппа более тренированных лиц численно мала (n = 6) и для анализа системы двух специалистов в разделе 3.6 оценки являются эксплоративными. С другой стороны, гетерогенность обернулась методологическим преимуществом: она позволила проверить обобщаемость модели на широком диапазоне физической подготовленности и количественно обнаружить эффект подгруппы тренированных, ставший центральной диагностической находкой разделов 3.5–3.6 и 3.8. На однородной выборке триатлонистов этот эффект остался бы невидимым: при сжатом диапазоне *time_to_lt2* критерий Манна–Уитни не дал бы статистически значимого различия per-subject MAE между подгруппами.

Подача гетерогенности как методологического преимущества не означает, что она была изначальным замыслом; это академически прозрачная двойственная характеристика выборки, ограничения которой зафиксированы в разделе 3.9 и в заключении (глава 4).

# 4. Заключение

Каждый вывод сопровождён ссылкой на конкретный результат разделов 3.1–3.8.

## 4.1 Выводы по задачам ТЗ

### 4.1.1 Задача 3: сформировать набор признаков ЭМГ, NIRS и ВСР для онлайн-предсказания близости к лактатным порогам

Зафиксирован информативный вклад каждой модальности по целевым переменным (раздел 3.2, таблица 3.2): для LT2 — ВСР (субъект-взвешенная медианная MAE 1,90 мин, ρ = 0,91, R² = 0,82); для LT1 — комбинация ЭМГ+NIRS (MAE 2,11 мин, ρ = 0,74, R² = 0,52). Установлено, что добавление третьей модальности к ведущей не даёт значимого улучшения ни для LT2 (HRV vs EMG+NIRS+HRV, p = 0,966), ни для LT1 (EMG+NIRS vs EMG+NIRS+HRV, p = 0,442) — раздел 3.2, таблица 3.3. Установлено также, что для нейросетевых архитектур абсолютные признаки эквивалентны их отсутствию в 86 % конфигураций (раздел 3.4.1, таблица 3.5), тогда как для линейного семейства абсолютный признак *hrv_mean_rr_ms* несёт 44,6 % суммарного веса коэффициентов Ridge (раздел 3.4.2, таблица 3.8) и его удаление снижает R² с 0,82 до 0,68.

### 4.1.2 Задача 4: разработать ML-модель оставшегося времени до LT1 и LT2; сопоставить baseline-подход на классических моделях с ручными признаками и нейросетевые архитектуры для временных рядов

Построен и количественно сопоставлен набор из десяти семейств моделей: один baseline на классических моделях с ручными признаками (Ridge, ElasticNet, SVR, GBM; раздел 3.1) и девять нейросетевых архитектур для временных рядов (раздел 2.8 методов). На LOSO-валидации с n = 18 испытуемыми установлено, что линейное семейство (состав раздел 3.1) статистически значимо превосходит все девять нейросетевых архитектур (перечень см. 3.9.2) — суммарно 90 значимых побед и 0 значимых поражений в 14 из 16 групп «целевая переменная × модальность × вариант» по критерию Уилкоксона с поправкой Холма (см. 3.3, таблица 3.4). Постобработка фильтром Калмана (раздел 2.10) для финальных моделей систематического выигрыша не дала.

### 4.1.3 Задача 5: оценить точность предсказания LT1 и LT2 относительно капиллярного лактата (LOSO), провести SHAP-анализ и сопоставить с литературой

Точность финальных моделей оценена в трёх дополняющих представлениях (раздел 2.9.1): субъект-взвешенная медианная MAE, raw window-weighted MAE, медиана по окнам. Полные значения по победителям приведены в разделе 3.1 (таблица 3.1) и в дополнительной структуре ошибки в разделе 3.5 (таблицы 3.9, 3.10). Установлено: для LT2 ошибка стабильна на всех бинах времени до порога (1,5–2,5 мин), для LT1 — стабильна на бинах ≤ 480 с (1,5–2,8 мин) и возрастает до 6,2–7,1 мин в бине > 480 с до порога (раздел 3.5.2, таблица 3.10). Установлено также, что эта структура ошибки концентрируется в подгруппе тренированных испытуемых: критерий Манна–Уитни для LT1 на всех трёх топовых моделях даёт p = 0,0002…0,003 (раздел 3.5.1, таблица 3.9), для LT2 — p = 0,35…0,68.

Анализ важности признаков методом SHAP (раздел 3.7) выполнен для четырёх финальных моделей: для LT2-победителей — на Ridge с линейный объяснитель, для LT1-победителей — на фактической модели SVR (победитель LT1, раздел 3.1) с пермутационный объяснитель. Для LT2 / HRV 75 % суммарной важности приходится на два признака — *hrv_mean_rr_ms* (43,5 %) и *hrv_dfa_alpha1* (31,4 %); для LT1 / EMG+NIRS доминирующего признака нет (ведущий — *trainred_hhb_std*, 5,2 %; суммарная доля топ-10 — 37,1 %), вклад распределён между EMG (54,0 % по группе), NIRS (40,3 %) и кинематикой (5,7 %). В мультимодальной модели LT1 / EMG+NIRS+HRV первым по важности оказывается *hrv_mean_rr_ms* (4,9 %), доля группы HRV — 11,6 %.

Сопоставление с литературой выполнено в разделе 3.9 по шести направлениям: диссоциация модальностей по порогам (3.9.1), доминирование линейного семейства при малом n (3.9.2), зависимость роли абсолютных признаков от класса модели (3.9.3), эффект подгруппы тренированных (3.9.4), диффузность LT1 и концентрированность LT2 в SHAP (3.9.5), гетерогенность выборки (3.9.6).

### 4.1.4 Дополнительные результаты сверх задач ТЗ

Проверена гипотеза о двух специалистах для LT1 (раздел 2.8.9, раздел 3.6; определения см. в 3.6). Установлено: симметричный вариант не превосходит единую обобщённую модель ни при идеальной маршрутизации (p = 0,246), ни при реальной (p = 0,899). Асимметричный вариант значимо превосходит единую модель при идеальной маршрутизации (p = 0,034), при реальной маршрутизации даёт тренд к значимости (p = 0,074, MAE_med 3,15 → 2,18 мин). Точность маршрутизатора на базальных признаках — 66,7 %, что совпадает с базовой частотой большего класса.

Дополнительно построены LOSO split-conformal интервалы для четырёх финальных моделей в обеих категориях признаков — итого 16 строк (раздел 2.11.4, раздел 3.8, таблица 3.17). Эмпирическое покрытие отклоняется от целевого в пределах 0,2–2,2 п.п. Полуширина 80 %-интервалов с абсолютными признаками: LT2 / HRV — ±3,49 мин, LT1 / EMG+NIRS — ±6,99 мин. Удаление абсолютных признаков расширяет интервалы LT2 (для HRV — до ±4,87 мин, +1,38 мин) и не меняет интервалы LT1 (для EMG+NIRS — ±6,59 мин, даже сужение на 0,40 мин).

## 4.2 Научная новизна

Каждый пункт новизны сопровождён ссылкой на конкретный результат настоящей работы; формулировки ограничены тем, что подкреплено собственным результатом.

1. Количественно показана зависимость минимально достаточного набора сенсоров от типа порога: для LT2 одной модальности ВСР достаточно (MAE 1,90 мин; добавление ЭМГ и NIRS не значимо, p = 0,966); для LT1 ведущей является комбинация ЭМГ+NIRS (MAE 2,11 мин; добавление ВСР не значимо, p = 0,442). Раздел 3.2.
2. Установлено, что выбор «абсолютные признаки vs тренды» зависит от класса модели: для нейросетевых архитектур — эквивалентность в 86 % конфигураций, для линейного семейства — статистически значимое преимущество with_abs на LT2 для модальности HRV (Δ_median MAE = +0,54 мин; p = 0,006) и для мультимодальной EMG+NIRS+HRV (Δ = +0,53; p = 0,034); ведущий абсолютный признак — *hrv_mean_rr_ms* (доля 44,6 % коэффициентов Ridge HRV / LT2). Для LT1 абляция линейного семейства даёт эквивалентность во всех модальностях. Раздел 3.4.
3. Получено эмпирическое подтверждение, что при n = 18 регуляризованная линейная регрессия на ручных признаках статистически значимо превосходит все девять протестированных нейросетевых архитектур (90 значимых побед, 0 поражений в 14 из 16 групп «целевая переменная × модальность × вариант»; парный критерий Уилкоксона с поправкой Холма). Раздел 3.3.
4. Описан и количественно охарактеризован эффект подгруппы тренированных для предсказания LT1: per-subject MAE значимо выше на тренированных (p = 0,0002…0,003); 79 % окон с |err| > 7 мин приходятся на эту подгруппу. Эффект отсутствует для LT2 (p = 0,35…0,68). Разделы 3.5, 3.8.
5. Показано, что для LT1 при n = 18 симметричное разделение модели на двух специалистов не работает, тогда как асимметричный вариант (исключение нетипичной подгруппы из обучающей выборки модели большинства) даёт значимый выигрыш при идеальной маршрутизации (p = 0,034) и тренд при реальной (p = 0,074). Раздел 3.6.

## 4.3 Практическая значимость

1. Минимальная конфигурация датчиков по целевым переменным: для онлайн-предсказания LT2 достаточно носимого кардиомонитора с регистрацией RR-интервалов; для LT1 — комбинации поверхностной ЭМГ и NIRS на работающей мышце. Раздел 3.2.
2. Для нейросетевых архитектур индивидуальная калибровка абсолютных норм физиологических сигналов не требуется; для линейного семейства калибровка *hrv_mean_rr_ms* (средний RR на калибровочной ступени) необходима. Раздел 3.4.
3. Для LT2 предсказательные интервалы практически применимы: полуширина ±3,49 мин при 80 %-покрытии (LOSO split-conformal, n = 18). Для LT1 интервалы шире — ±6,99 мин при 80 %, ±9,05 мин при 90 % — и в текущей версии не достаточны для оперативного использования. Раздел 3.8.

## 4.4 Ограничения

1. Объём выборки n = 18 испытуемых (записано 19, одна запись исключена из-за низкого качества сигнала). Все результаты следует рассматривать в этом масштабе; для отдельных нейросетевых архитектур по целевой переменной LT2 число субъектов в LOSO составило 16–17 из-за внутренних *dropna* (раздел 3.1, таблица 3.1).
2. Выборка гетерогенная и отклонилась от исходно заявленной целевой популяции — триатлонистов (раздел 1.7 обзора литературы). В связи с трудностями набора целевой группы она была расширена до лиц с различным уровнем физической подготовки, что ограничивает прямое сопоставление полученных численных показателей с опубликованными нормами лактатного порога для популяции профессиональных триатлонистов. Развёрнутая интерпретация двойственного характера этого отклонения приведена в разделе 3.9.6.
3. Подгруппа тренированных (n = 6) численно мала. Результаты по системе двух специалистов (раздел 3.6) являются эксплоративными.
4. Маршрутизатор в системе двух специалистов достигает точности 66,7 % — на уровне базовой частоты большего класса. Это главное узкое место системы; устранение требует расширения выборки и дополнительных базальных признаков.
5. Для LT1 LOSO split-conformal интервалы имеют полуширину ±7–9 мин (раздел 3.8). При текущем объёме данных интервал недостаточно узок для оперативного использования по аэробному порогу.
6. Расхождение между тремя представлениями MAE сохраняется и должно учитываться при цитировании числовых значений: для LT1 / EMG+NIRS субъект-взвешенная медианная MAE равна 2,11 мин, медиана |err| по окнам — 3,33 мин, raw window-weighted MAE — 4,25 мин (разделы 3.1 и 3.5.2).

## 4.5 Направления дальнейших исследований

1. Расширение выборки, в первую очередь — подгруппы тренированных, для проверки гипотезы о двух симметричных специалистах и устранения главного ограничения системы двух специалистов (раздел 3.6).
2. Поиск концентрированного физиологического маркера LT1 — агрегированного признака, способного синтезировать вклад множества слабых сигналов в одну величину. Обоснование — диффузный SHAP-профиль LT1 (раздел 3.7.3, 3.9.5).
3. Перевод маршрутизатора подгрупп на более информативные базальные признаки; в текущей версии маршрутизатор работает на уровне базовой частоты классов (раздел 3.6).
4. Расширение конформной калибровки от глобальной к стратифицированной по подгруппам тренированности — для отражения структурной неоднородности per-subject ошибки LT1 в виде различий полуширин (раздел 3.8).
5. Расширение SHAP-интерпретации на нейросетевые архитектуры (DeepExplainer / Integrated Gradients для TCN, Attention-LSTM, вейвлет-семейства). Это позволит количественно сопоставить, какие признаки используют классы моделей, для которых не выявлено превосходства над линейным семейством (раздел 3.3), и проверить гипотезу о структурно ином использовании временного контекста (раздел 3.9.3).
