<!-- АВТОСИНХРОНИЗИРОВАНО из Тамразов.docx (разделы 2.8.3–2.8.6) скриптом scripts/sync_methods_from_docx.py. Ручные правки будут перезаписаны при следующей синхронизации. -->

### 2.8.3 Формирование обучающей выборки

Для таргета LT2 в обучающую выборку включались окна с валидной разметкой времени до порога и при наличии сигналов достаточного качества во всех модальностях одновременно. Для таргета LT1 дополнительно использовался признак пригодности окна, формируемый на этапе разметки датасета.

После фильтрации датасет содержит 4 130 окон для LT1 и 3 651 окно для LT2, распределённых по 18 испытуемым.

Калибровочная ступень нагрузки (30 Вт) исключена из обучающих окон: её отсчёты используются исключительно для вычисления базальных физиологических показателей (средние значения ЧСС, ВСР, SmO₂ и ЭМГ-активности в состоянии покоя), которые служат входными признаками маршрутизатора в системе двух специалистов (раздел 2.8.9). Включение калибровочной ступени в обучающие окна создало бы утечку данных: целевая переменная  не определена на этой ступени, а характеристики сигнала на 30 Вт систематически отличаются от таковых при нагрузке.

### 2.8.4 Методология широкого перебора

При малом числе испытуемых (n = 18) нет оснований заранее отдавать предпочтение более сложным архитектурам перед линейными моделями. Поэтому в работе применялась стратегия систематического перебора: одно линейное семейство с зоопарком из классических алгоритмов sklearn и девять нейросетевых архитектур, охватывающих четыре различных подхода к обработке временных рядов. Линейное семейство тестировалось на всех пяти наборах признаков, нейросетевые архитектуры — на четырёх (модальность HRV в отдельности не подавалась в нейросети), каждый набор — в двух вариантах (with_abs / noabs) и для двух таргетов независимо. Суммарно это даёт 164 конфигурации в финальном рейтинге: (1 линейное семейство × 5 наборов + 9 нейросетевых архитектур × 4 набора) × 2 варианта × 2 таргета.

Широкий перебор — это явный методологический выбор: он позволяет ответить на вопрос, насколько нейросетевая сложность оправдана при данном объёме выборки.

### 2.8.5 Схема валидации

В качестве единственной схемы валидации во всех экспериментах применялась стратегия Leave-One-Subject-Out (LOSO). Суть метода: на каждой итерации один испытуемый целиком выносится в тестовое множество, остальные 17 формируют обучающее множество; процедура повторяется для каждого из 18 испытуемых.

Выбор LOSO обусловлен целевой областью применения модели: предсказание должно работать на новом, ранее не встречавшемся человеке, а не только на субъектах, чьи данные присутствовали при обучении. При n = 18 испытуемых LOSO максимизирует объём обучающего множества на каждой итерации.
