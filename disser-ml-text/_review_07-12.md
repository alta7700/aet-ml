# Журнал правок текстов 07–12 (Этапы 2, 3, 4.1, 4.2)

## Этап 2: устранение повторов

- text_07/3.1 (абз. 1) — Тема 4: расширено определение трёх вариантов MAE (subject-weighted median, window-median, raw window-weighted) при первом появлении.
- text_08/3.4.3 (последний абзац) — Тема 8: «Механизм расхождения по таргетам и классам моделей …» сокращён до короткого вердикта со ссылкой на 3.9.3.
- text_10/3.7 (вводный абзац) — Тема 3: сокращены параметры моделей (α, C, ε), добавлена ссылка на примечание к табл. 3.1.
- text_11/3.9.1 (абз. 1) — Тема 7: убраны числовые значения MAE/ρ/R²/p (заменены ссылкой на 3.2); SHAP-фрагмент сохранён как интерпретация.
- text_11/3.9.2 (абз. 1) — Темы 1 и 11: убрано перечисление Ridge/EN/Huber/GBM/SVR, заменено ссылкой на 3.1; числа 39/51/90 заменены ссылкой на 3.3, табл. 3.4.
- text_11/3.9.2 (последний абзац) — Тема 3: убрано перечисление победителей с модальностями и параметрами; оставлен содержательный вывод про линейные/нелинейные модели.
- text_11/3.9.3 (абз. 1) — Тема 8: убраны числовые значения p, Δ, доли 44,6 %, заменены ссылкой на 3.4.2, табл. 3.7–3.8.
- text_11/3.9.4 (абз. 1) — Тема 6: сокращены p-значения и числа Манна–Уитни, заменены ссылкой на 3.5.1.
- text_11/3.9.4 (абз. 1) — Тема 5: убрано раскрытие «раздельные модели для тренированных и нетренированных» и «специалист на 12 нетренированных + generalist для тренированных», заменено ссылкой на 3.6.
- text_11/3.9.5 (абз. 1) — Тема 10: сокращены числа 54,0/40,3/5,7/61,7/33,7 %, заменены ссылкой на табл. 3.13, 3.15–3.16.
- text_12/4.1.2 — Темы 1, 2, 11: убрано развёрнутое перечисление линейных моделей и девяти нейросетевых архитектур, числа 39/51, заменено ссылками на 3.1, 3.9.2, 3.3.
- text_12/4.1.3 (абз. 2) — Тема 3: убраны параметры SVR (C, ε, RBF), оставлена ссылка на 3.1.
- text_12/4.1.4 — Тема 5: убрано раскрытие симметричного/асимметричного вариантов, заменено ссылкой на 3.6.
- text_12/4.4 (п. 2) — Тема 12: убрано раскрытие «двойственности» (с одной стороны…, с другой стороны…), заменено ссылкой на 3.9.6.

Итого: 14 правок по 9 темам из аудита (темы 2, 9 — без правок: канон уже работает корректно; тема 11 — частично совмещена с темой 1 в 3.9.2).

## Этап 3: сборка

- Создан `chapter_3_results_FULL.md` — конкатенация шести файлов в порядке 07 → 08 → 09 → 10 → 11 → 12.
- Добавлен заголовок `# 3. Результаты` в самом начале.
- Заголовок `# 4 Заключение — ML-составляющая` заменён на `# 4. Заключение по ML-части`.
- Удалены шесть финальных блоков «**Оставшиеся маркеры:** …» и предшествующие разделительные `---`.
- Существующая нумерация таблиц (3.1, 3.2, 3.3 + 3.3-bis, 3.4, 3.5, 3.6, 3.7, 3.8, 3.9, 3.10, 3.11, 3.12, 3.13, 3.14, 3.15, 3.16, 3.17) и рисунков (3.1, 3.2 + 3.2-bis, 3.3, 3.3-bis, 3.4, 3.4-bis, 3.5, 3.5-bis, 3.6, 3.7, 3.8, 3.9, 3.10, 3.11, 3.12, 3.13) сквозная — не перенумеровано.
- Объём собранного документа: 527 строк, 14 разделов 2-го уровня, 19 подразделов 3-го уровня, 18 строк-подписей таблиц, 17 подписей рисунков.
- Ссылки «см. раздел 3.X» проверены, явных правок не потребовалось — новая структура совпадает с прежней нумерацией.

## Этап 4.1: замена кодов

- `v0011` — удалён из текстовых упоминаний: «(v0011, раздел 2.8.6)» → «(раздел 2.8.6)»; «линейного семейства (v0011)» → «линейного семейства»; «v0011 в варианте per-window» → «в варианте per-window»; «Таблица 3.4. Доминирование линейного семейства (v0011)» → без скобки; в Рисунке 3.3-bis и 3.4-bis «значимых побед v0011» → «значимых побед линейного семейства»; «для v0011 — 10 панелей» → «для линейного семейства»; в строках таблицы 3.9 удалены `(v0011)` (5 строк). Внутри пути `results/v0011/...` удаление произведено на Этапе 4.2.
- `v0102` — заменён на TCN в фрагменте «v0102 на LT2 / EMG+NIRS+HRV» (text_08, абз. 31).
- `v0107` — удалён из строк таблиц 3.6 (3 строки) и 3.9 (2 строки); таблица 3.1 содержит первое именное упоминание «ансамбль (вейвлет-TCN + ElasticNet)», далее в тексте только «ансамбль».
- `Ensemble` (англ.) заменено на «ансамбль» в таблицах 3.1, 3.6, 3.9, 3.10 (9 строк).
- Параметры моделей: SVR (C = 100, ε = 1,0) — оставлены при первом упоминании в примечании к табл. 3.1 (раздел 3.1), удалены повторы в text_09/3.6 («фактический победитель LT1 — SVR на EMG+NIRS») и в text_09/3.5.1 (кросс-таргетная корреляция). В заголовках SHAP-таблиц 3.13–3.16 параметры сохранены как идентификатор конфигурации.

## Этап 4.2: удаление локальных ссылок

- Удалены строки «Источник: [results/...](...). …» — 16 строк (целиком).
- Удалены строки «Файл: [results/...](...). Место в тексте: …» из подписей рисунков — 17 строк (целиком), подписи рисунков сохранены.
- Удалены inline-ссылки «([results/...](...))» и «([final-plan/...](...))» — 6 вхождений (5 results + final-plan/step_02a, step_05, step_06).
- Удалён фрагмент «`results/v0011/best_per_set.csv`» в скобках — 1 вхождение.
- Удалён хвост «; сводно — [results/...](...)» в строке-источнике таблицы 3.3 — 1 вхождение (вместе с inline).
- Удалён хвост «, ссылка на step_06» в text_12/4.4 п. 6 — 1 вхождение.
- Подчищены двойные пустые строки.

Итого по Этапу 4.2: примерно 42 удаления локальных путей/ссылок. Сохранены: ссылки на таблицы 3.X, рисунки 3.X, разделы 2.X и 3.X, Приложение А-2, Тамразов.docx, имена признаков в обратных кавычках.

## Подозрительные места для следующего стилистического агента

- text_11/3.9.1 (после удаления повтора): первый абзац теперь начинается с «Диссоциация модальностей зафиксирована в подразделе 3.2…», далее сразу «SHAP-анализ (подраздел 3.7) согласуется с этой картиной…». Связка между фактом из 3.2 и SHAP-фрагментом стала плотнее, но интонационно может потребовать сглаживания.
- text_11/3.9.2 (абз. 1): после удаления статистики «39 / 51 / 90 побед» абзац стал короче — связка с последующим параграфом про литературу (Zeng, Miura и др.) корректна, но переход «не противоречит, а уточняет литературу» теперь идёт сразу за ссылкой, без числовой опоры. Стоит проверить, нужна ли вступительная фраза.
- text_11/3.9.3 (абз. 1): после сокращения остался один сплошной тезис «эффект концентрирован на LT2 и проходит через `hrv_mean_rr_ms` (см. 3.4.2)» — затем сразу conformal-интервалы. Возможно, потребуется добавить связку между ablation и conformal-частью.
- text_11/3.9.4 (абз. 1): объединены сокращения по Теме 5 и Теме 6 — абзац стал заметно короче, но смысловые блоки идут в одном предложении друг за другом. Желательна редактура для лучшей логической связки.
- text_11/3.9.5 (абз. 1): после удаления Ridge-проекционных чисел переход от «концентрированный / диффузный» к «SVR извлекает дополнительный сигнал…» теперь сделан без переходного оборота — стилистически шероховато.
- text_12/4.4 (п. 2): после сокращения остался только тезис «Выборка гетерогенна; интерпретация двойственна — см. 3.9.6.» Возможно, ограничивающий пункт стоит расширить одной короткой строкой о том, в чём проявляется ограничение, без дублирования 3.9.6.
- text_08/3.4.3 (последний абзац): после сокращения «Механизм расхождения по таргетам и классам моделей» сводный вердикт заканчивается одной строкой-ссылкой на 3.9.3. Логически это правильно, но абзац перед рисунком 3.5-bis стал коротким и обрывистым.

## Этап 4.3–4.4: научный стиль и финальная вычитка

Правки выполнены в файле `disser-ml-text/chapter_3_results_FULL.md`.

### Статистика правок по 4.3 (по пунктам a–l)

- **a) Лицо и стиль повествования** — 0 явных правок (запретные слова «давайте», «попробуем», «довольно», «отличный», «к сожалению», «увы», «к слову», «получается так» не найдены в собранном тексте). Тон уже выдержан в безличной форме / 1 л. мн. ч.
- **b) Времена** — без правок. Времена согласованы: прошедшее для процедур, настоящее для свойств моделей и интерпретаций.
- **c) Числовой формат** — без правок. Десятичные запятые, пробелы перед «мин», «с», «%», формат `p = 0,XXX` уже единообразны.
- **d) Единообразие терминов** — около 15 правок: «таргет/таргета/таргетов/таргете/таргеты/таргетам» → «целевая переменная» в текстовых употреблениях (с сохранением `target × feature_set × variant` только в шапке таблицы 3.4); «пайплайном / пайплайна» → «конвейером / конвейера» (2 правки); «baseline» → «опорная модель» (3 правки); «conformal-интервалы / -калибровка / -полуширин / -покрытия» → «конформные …» (5 правок), при этом «LOSO split-conformal» оставлено как название метода; «paired Wilcoxon» в тексте раздела 3.4.2 → «парный критерий Уилкоксона». Введено «целевая переменная × модальность × вариант» как русский эквивалент технической оси.
- **e) Аббревиатуры** — 3 расшифровки добавлены в первом абзаце 3.1: ЭМГ (EMG), NIRS, ВСР (HRV), MAE (mean absolute error), R² (коэффициент детерминации), LOSO (leave-one-subject-out, кросс-валидация с исключением одного испытуемого).
- **f) Заголовки** — без правок: точек в конце нет, эмфазы внутри нет, иерархия `# / ## / ###` соблюдена.
- **g) Таблицы** — 18 подписей таблиц переведены из формата `**Таблица 3.X.** Название.` в формат `**Таблица 3.X — Название**` (длинное тире, без концевой точки).
- **h) Рисунки** — 17 подписей рисунков переведены из формата `*Рисунок 3.X.* Название.` в формат `*Рисунок 3.X — Название*`.
- **i) Перечни** — без правок. Маркированные списки оправданы там, где присутствуют (5 конфигураций двухэкспертной схемы; пункты ограничений и научной новизны).
- **j) Английские вкрапления** — см. пункт d. Дополнительно сохранены: with_abs / noabs (служебные коды вариантов, расшифрованы в скобках при первом введении в 3.1); window-median MAE и raw window-weighted MAE (технические термины, при первом введении дан русский эквивалент); ablation, generalist, real_router_*, oracle_*, dropna, gap — оставлены как технические идентификаторы.
- **k) Цитирование числовых утверждений** — все ключевые числа (1,90 / 2,11 / 2,38 / 3,33 / 4,25 / 90 побед / 14 из 16 / n = 18 / p = 0,034 / p = 0,074) подкреплены ссылками на таблицы и подразделы; ссылка п. 6 в подразделе 4.4 переведена с подраздела 3.8 на подразделы 3.1 и 3.5.2 (где соответствующие числовые значения фактически приведены).
- **l) Запятые, кавычки, тире** — «ёлочки» в тексте уже использовались; прямые кавычки `"` в файле не найдены. Длинное тире `—` для пояснений выдержано, диапазоны через короткое тире без пробелов («1,42–2,38») присутствуют.

### Финальная сквозная вычитка (4.4)

- Параметры SVR (C = 100, ε = 1,0) удалены из столбца «Модель» таблицы 3.1 (две строки LT1) — теперь параметры даны ровно один раз, в примечании к таблице. В заголовках SHAP-таблиц 3.15–3.16 параметры SVR сохранены как идентификатор конфигурации (журнал этапа 4.1 это зафиксировал).
- Параметры Ridge (α = 1000) встречаются повторно в подразделе 3.6 («единая базовая модель — Ridge (α = 1000) на модальности EMG+NIRS») — это второе содержательное упоминание именно конфигурации двухэкспертной схемы, где параметр выступает идентификатором базовой модели специалиста, а не повторным определением. Оставлено как осмысленное идентифицирующее упоминание.
- Согласованность чисел проверена: 1,90 / 2,11 / 2,38 / 3,33 / 4,25 / 90 побед / 0 поражений / 14 из 16 групп / n = 18 / p = 0,034 / p = 0,074 встречаются в одной форме во всех разделах.
- Сглажены «рваные» места: 3.9.1 (добавлена связка «Эта картина воспроизводится и на уровне индивидуального вклада признаков»), 3.9.2 (вступление «Зафиксированное доминирование не противоречит…» вместо «Этот результат не противоречит…»), 3.9.3 (добавлена связка «Та же асимметрия проявляется и в ширине предсказательных интервалов»), 3.9.4 (добавлено «Этот эффект далее воспроизводится в проверке двухэкспертной схемы», раскрыто «paired Wilcoxon vs generalist» → «парный критерий Уилкоксона относительно generalist», «симм./асимм.» → полные формы), 3.9.5 (вводная фраза «Сопоставление двух способов разложения вкладов уточняет картину»), 3.4.3 (последняя строка-ссылка расширена пояснением, почему именно по линейной/нейросетевой оси расходится механизм), 4.4 п. 2 (добавлено описание, в чём проявляется ограничение).
- Ссылки «см. раздел 3.X / таблица 3.X / рисунок 3.X» проверены: все целевые объекты в файле существуют. Дополнительная правка п. 6 в подразделе 4.4 (ссылка 3.8 → 3.1 и 3.5.2) — числовые значения трёх вариантов MAE действительно представлены именно в этих двух подразделах.

### TODO для пользователя

TODO-комментариев в тексте не оставлено. Все обнаруженные стилистические и логические шероховатости устранены без необходимости содержательных решений. Числовые значения, параметры моделей и нумерация — без правок (по запрету).

### Итоговая рекомендация

Текст готов к вставке в `.docx`-шаблон без дополнительных автоматизированных проходов. Дальнейшие правки — на стороне пользователя: ручной контроль типографики (неразрывные пробелы перед единицами измерения при экспорте в Word; единообразие тире после автозамены Word), вставка изображений по подписям `*Рисунок 3.X — …*` и финальная сверка цитирований по списку литературы.
