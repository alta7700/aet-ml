<!-- АВТОСИНХРОНИЗИРОВАНО из Тамразов.docx (разделы 2.11–3) скриптом scripts/sync_methods_from_docx.py. Ручные правки будут перезаписаны при следующей синхронизации. -->

## 2.11 Статистический анализ и интерпретация

### 2.11.1 Парные сравнения моделей: критерий Уилкоксона с поправкой Холма

Для сравнения двух произвольных конфигураций моделей (по версии, набору признаков или варианту) применялся парный критерий знаковых рангов Уилкоксона (двусторонний). Тест строился на векторах per-subject MAE: для каждого испытуемого вычислялась MAE на его тестовом фолде, и сравнивались пары значений MAE одного и того же испытуемого под двумя конфигурациями.

При проведении множественных попарных сравнений внутри одной группы (target × feature_set × variant) применялась поправка Холма (Holm–Bonferroni) на контроль familywise error rate. Порог значимости: α = 0,05.

### 2.11.2 Сравнение независимых групп: критерий Манна–Уитни

Для проверки различий между тренированными и нетренированными испытуемыми применялся критерий Манна–Уитни (Mann–Whitney U-test, двусторонний), поскольку эти подгруппы являются независимыми выборками. Сравнивались распределения per-subject MAE внутри двух подгрупп: 6 тренированных (время до LT2 более 13 мин) и 12 нетренированных.

### 2.11.3 Исключение абсолютных значение: критерий эквивалентности

Для оценки практической значимости вариантов with_abs и noabs использовалась операциональная предметная граница эквивалентности:

- Различие признаётся несущественным («эквивалентно»), если одновременно выполняются:  мин И  (парный Уилкоксон на per-subject MAE);

- При нарушении хотя бы одного условия выносится вердикт «лучше with_abs» или «лучше noabs» в зависимости от знака .

Порог 0,3 мин (~18 с) отражает минимальное клинически различимое улучшение: отклонение меньше этого значения не изменяет практических рекомендаций по выбору набора сенсоров.

### 2.11.4 Конформное предсказание (conformal prediction)

Точечное предсказание модели не даёт информации о его надёжности: для клинического применения важно знать не только ожидаемое время до порога, но и диапазон, в который оно попадёт с заданной вероятностью. Для построения таких предсказательных интервалов с гарантированным покрытием применялся метод LOSO split-conformal prediction.

Метод основан на предположении об однородности выборки: ошибки модели на тестовом субъекте имеют то же распределение, что и на калибровочных. При однородной выборке испытуемых это допущение выполняется, и гарантия покрытия 1−α обоснована теоретически без параметрических предположений о форме распределения ошибок.

Тестировались два уровня покрытия: 90% (α=0,1) и 80% (α=0,2). Уровень покрытия задаёт, какую долю тестовых окон интервал должен накрывать: при 90% истинное значение должно попадать в интервал не менее чем в 9 случаях из 10. Более высокий уровень даёт более широкий интервал.

### 2.11.5 Интерпретация модели: анализ важности признаков (SHAP)

Чтобы понять, какие физиологические сигналы вносят наибольший вклад в предсказание времени до порога, применялся метод SHAP (SHapley Additive exPlanations). Для каждого окна SHAP разбивает предсказание модели на вклады отдельных признаков: положительный вклад означает, что признак сдвигает предсказание вверх (дальше от порога), отрицательный — вниз (ближе к порогу).

Важность каждого признака оценивалась как среднее абсолютное значение SHAP по всем тестовым окнам всех субъектов . Дополнительно признаки объединялись в группы по модальности (EMG, NIRS, HRV, кинематика, взаимодействия) — это позволяет оценить относительный вклад каждого источника сигнала в целом, а не отдельных признаков.
